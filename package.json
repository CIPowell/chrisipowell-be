{
  "name": "root",
  "private": true,
<<<<<<< HEAD
=======
  "workspaces": {
    "packages": [
      "services/*"
    ],
    "nohoist": [
      "**/contentful",
      "**/@contentful/**",
      "**/winston",
      "**/aws-xray-sdk-core"
    ]
  },
>>>>>>> b433bad (Add excution test)
  "scripts": {
    "postinstall": "for dir in services/*; do (cd \"$dir\" && npm install); done",
    "test": "for dir in services/*; do (cd \"$dir\" && npm test); done",
    "build": "for dir in services/*; do (cd \"$dir\" && npm build); done",
    "deploy": "for dir in services/*; do (cd \"$dir\" && npm deploy); done",
    "release": "for dir in services/*; do (cd \"$dir\" && npm release); done",
    "sonar": "sonar-scanner"
  },
  "version": "2.0.0",
  "devDependencies": {
    "sonarqube-scanner": "^2.8.0"
  }
}
